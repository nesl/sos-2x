#ifndef _SENSOR_API_DRIVER_H_
#define _SENSOR_API_DRIVER_H_

// Definitions for interface between the
// sensor API and sensor driver module.

// This file should only be included by sensor API
// and sensor driver modules.

/*
// Control interface for interaction between sensor API
// and sensor drivers, and hardware drivers and sensor drivers.
typedef enum {
	SENSOR_REGISTER_REQUEST_CMD	= 0,
	SENSOR_REMOVE_REQUEST_CMD,
	SENSOR_GET_DATA_CMD,
	SENSOR_STOP_DATA_CMD,
	SENSOR_ENABLE_CMD,
	SENSOR_DISABLE_CMD,
} sensor_driver_command_t;
*/
#endif

